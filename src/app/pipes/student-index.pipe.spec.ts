import { StudentIndexPipe } from './student-index.pipe';

describe('StudentIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
